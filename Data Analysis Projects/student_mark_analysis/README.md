# 🧮 Student Marks Analysis using Pandas

### 🎯 Project Overview
This beginner-level data analysis project explores student exam performance using **Python**, **Pandas**, and **Matplotlib**.  
It demonstrates how to load, summarize, and visualize structured data efficiently.

---

### 📂 Files in this Project
| File | Description |
|------|--------------|
| `student_marks.csv` | Dataset containing students' names and marks in three subjects |
| `student_marks_analysis.py` | Python script that reads, analyzes, and visualizes the dataset |
| `README.md` | Project documentation |

---

### 🧠 Concepts Covered
- Reading and analyzing CSV files using **Pandas**
- Generating descriptive statistics (`.info()` and `.describe()`)
- Creating new columns (`Total`, `Average`)
- Sorting and ranking data (`.sort_values()`)
- Finding top performer using `.idxmax()` and `.loc[]`
- Visualizing results with **Matplotlib** bar charts

---

### 🧩 Features
✅ Calculate subject-wise averages  
✅ Compute total and average marks per student  
✅ Identify the top-scoring student  
✅ Sort and rank all students by their average marks  
✅ Visualize the results using a clean, colorful bar chart  

---

### 🚀 How to Run the Project
1. Make sure you have **Python 3.x** installed.  
2. Install dependencies:
   ```bash
   pip install pandas matplotlib

